const authConfig = {
    providers: [
        {
            domain: "https://living-ostrich-19.clerk.accounts.dev/",
            applicationID: "convex",
        },
    ]
};

export default authConfig;
